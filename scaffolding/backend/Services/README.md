# Services

Generated service interfaces and implementations are placed here by AICASE.

Services contain business logic, apply business rules defined in the project JSON, and coordinate repository calls. They are registered in `Program.cs` with `AddScoped`.
