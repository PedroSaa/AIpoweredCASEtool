# Models

Generated C# model classes are placed here by AICASE.

Each model corresponds to one entity defined in the project JSON and includes:
- Properties with correct C# types
- Data annotation attributes (`[Required]`, `[MaxLength]`, etc.)
- Navigation properties for entity relationships
