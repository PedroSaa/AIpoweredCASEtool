# Modelos

Esta carpeta contiene las clases de entidad generadas por AICASE.

Cada archivo sigue el patrón: `{NombreEntidad}.cs`

Los modelos incluyen:
- DataAnnotations para validación
- Navigation properties para relaciones
- Atributos de tabla y columna para mapeo con EF Core
