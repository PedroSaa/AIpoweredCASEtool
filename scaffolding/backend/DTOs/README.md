# DTOs

Generated Data Transfer Object classes are placed here by AICASE.

Each entity generates three DTOs:
- `{Entity}CreateDto` – fields accepted when creating a new record
- `{Entity}UpdateDto` – fields accepted when updating an existing record
- `{Entity}ResponseDto` – fields returned in API responses (may include computed/navigation fields)
