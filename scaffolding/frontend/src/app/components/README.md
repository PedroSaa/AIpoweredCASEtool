# Components

Generated Angular components are placed here by AICASE.

Each entity generates:
- `{entity}-list/` – list component with data grid, filters, pagination, and action buttons
- `{entity}-form/` – form component with reactive forms, field validation, and section layout
