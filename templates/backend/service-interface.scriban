{{~ # Service Interface template – AICASE ~}}
using {{ namespace }}.DTOs;
using {{ namespace }}.Models;

namespace {{ namespace }}.Services;

/// <summary>
/// Service interface for {{ entity.name }} business logic.
/// Generated by AICASE.
/// </summary>
public interface I{{ entity.name }}Service
{
    Task<(IEnumerable<{{ entity.name }}ResponseDto> Items, int Total)> GetAllAsync(
        int page = 1, int pageSize = 10,
        string? sortField = null, string? sortDirection = "asc");

    Task<{{ entity.name }}ResponseDto?> GetByIdAsync({{ entity.primary_key.csharp_type }} id);

    Task<{{ entity.name }}ResponseDto> CreateAsync({{ entity.name }}CreateDto dto);

    Task<{{ entity.name }}ResponseDto?> UpdateAsync({{ entity.primary_key.csharp_type }} id, {{ entity.name }}UpdateDto dto);

    Task<bool> DeleteAsync({{ entity.primary_key.csharp_type }} id);
}
