import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';
import { {{ entity.name }}ListComponent } from './{{ entity.angularfilename }}-list/{{ entity.angularfilename }}-list.component';
import { {{ entity.name }}FormComponent } from './{{ entity.angularfilename }}-form/{{ entity.angularfilename }}-form.component';

const routes: Routes = [
  { path: '', component: {{ entity.name }}ListComponent },
  { path: 'new', component: {{ entity.name }}FormComponent },
  { path: ':id', component: {{ entity.name }}FormComponent }
];

@NgModule({
  imports: [RouterModule.forChild(routes)],
  exports: [RouterModule]
})
export class {{ entity.name }}RoutingModule { }
